title: Events

columns:
  main:
    width: 3/3
    sections:
      events:
        type: pages
        headline: All Events
        status: all
        templates:
          - event
        sortBy: date desc
        parent: site.find("events") # get events from parent page
        limit: 20
  # sidebar:
  #   width: 1/3
  #   sections:
  #     settings:
  #       type: fields
  #       fields:
