title: Recurring Events

columns:
  main:
    width: 3/3
    sections:
      events:
        type: pages
        headline: All Recurring Events
        status: all
        templates:
          - recurring-event
        sortBy: date desc
        parent: site.find("recurring-events") # get events from parent page
        limit: 20
  # sidebar:
  #   width: 1/3
  #   sections:
  #     settings:
  #       type: fields
  #       fields:
